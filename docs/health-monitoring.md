# Health Monitoring
- Spring Boot Actuator health, liveness, readiness
- Prometheus scrape endpoint enabled
- Custom `reportingSubsystem` health indicator
- DB and Redis health contributors auto-enabled when dependencies are reachable
