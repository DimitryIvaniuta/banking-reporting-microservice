ServerRoot "/usr/local/apache2"
Listen 80
LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule dir_module modules/mod_dir.so
LoadModule mime_module modules/mod_mime.so
LoadModule headers_module modules/mod_headers.so
LoadModule rewrite_module modules/mod_rewrite.so
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so
LoadModule proxy_balancer_module modules/mod_proxy_balancer.so
LoadModule slotmem_shm_module modules/mod_slotmem_shm.so
LoadModule lbmethod_byrequests_module modules/mod_lbmethod_byrequests.so
ServerName localhost
PidFile /tmp/httpd.pid
User daemon
Group daemon
ErrorLog /proc/self/fd/2
CustomLog /proc/self/fd/1 combined

# True percentage canary (weighted split) + sticky cookie routing.
# - Weighted by request count using mod_proxy_balancer (e.g. 90/10)
# - Sticky via BRMSWAFID cookie, so a client stays on the same WAF after first assignment
# Adjust loadfactor values below to tune the canary percentage.
<Proxy "balancer://waf_cluster">
  BalancerMember "http://apache-httpd-waf-detect:8080" route=detect loadfactor=90
  BalancerMember "http://apache-httpd-waf-block:8080" route=block loadfactor=10
  ProxySet lbmethod=byrequests stickysession=BRMSWAFID
</Proxy>

<VirtualHost *:80>
  ServerName localhost
  ProxyPreserveHost On
  RequestHeader set X-Forwarded-Proto "http"

  # Keep /actuator hidden on canary router (only /api goes through router)
  RedirectMatch 404 ^/actuator(/|$)

  # Route API through weighted balancer (sticky cookie applied after first assignment)
  ProxyPass        /api/ balancer://waf_cluster/api/
  ProxyPassReverse /api/ balancer://waf_cluster/api/

  # Set sticky cookie when Apache assigns a backend route
  Header add Set-Cookie "BRMSWAFID=.%{BALANCER_WORKER_ROUTE}e; Path=/; HttpOnly; SameSite=Lax" env=BALANCER_ROUTE_CHANGED

  Header always set X-Canary-Router "weighted-sticky"
  Header always set X-Canary-Split "detect=90,block=10"
</VirtualHost>
